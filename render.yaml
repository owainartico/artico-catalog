services:
  - type: web
    name: artico-catalog
    env: static
    buildCommand: echo "No build needed"
    staticPublishPath: ./catalog
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
