# 2026-02-25

## Telegram Plugin
- Telegram plugin won't load — shows empty in Channels table
- Messages still work via direct API connection, not the plugin
- OpenClaw update to 2026.2.23 available but can't install — runs as SYSTEM, npm global is under User account
- **TODO when Owain is at PC:** `npm install -g openclaw@2026.2.23`
- If plugin still doesn't work after update, investigate further
- Gateway error spam "no configured channels detected" when sub-agents try to announce — plugins.allow has telegram but plugin doesn't load

## Skoocloud Import Task
- Built standalone Playwright script: `workspace/tasks/skoocloud-import.js`
- Test run successful: 14,738 chars CSV, 46 rows, 31 valid, Import Complete
- Scheduled via Windows Task Scheduler daily at 11:00 AM
- Sends Telegram notification on success/failure
- Removed duplicate OpenClaw cron job
- Chromium installed at: `C:\WINDOWS\system32\config\systemprofile\AppData\Local\ms-playwright\chromium-1208\chrome-win64\chrome.exe`

## Browser Setup
- OpenClaw browser tool doesn't work — gateway runs as SYSTEM, can't launch browser
- Headless Edge detected but fails to start via CDP
- Standalone Playwright scripts work fine as workaround

## System Config
- Disabled sleep, hibernate, screensaver, monitor timeout
- Brave Search API configured and working
- Domain whitelist: adobe.com, zoho.com, stackoverflow.com, github.com (self-enforced in TOOLS.md)

## Zoho Assist
- Tried to install for remote access, couldn't download installer (needs browser to trigger JS download)
- Chocolatey doesn't have it

## GitHub Repos Cloned
Token: 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 (read-only, 3 repos)
1. **artico-sales** — Node/Express + PostgreSQL on Render. Territory intelligence, visit logging, dashboards, alerts, scoreboard, KPIs, grading, call planner. Zoho Books OAuth integration. SPA frontend.
2. **factory-tracker** — Node/Express on Render. JSON file storage (not DB). Production pipeline: Ready → Printing → Heat Press → Laser Cut → QC → Packaging → Stock. Zoho integration for PO sync, billing, inventory adjustments. Supports Product, Display, and Custom order types.
3. **name-a-bright-star** — Star registration system. Node/Express + PostgreSQL. Customer-facing with Shopify integration.

## Architecture Notes
- Both sales + factory apps: Node/Express, deployed on Render, same Zoho OAuth pattern
- Factory uses JSON files for data (orders.json, users.json, audit.json) — no database
- Sales uses PostgreSQL with migrations in server.js
- Factory has Zoho PO sync, bill creation, purchase receives, inventory adjustments, QC write-offs
- Factory has 2 production paths: A (full) and B (laser only)
- Factory has auto-retry for failed Zoho syncs
- Next task: Build customisation tab in sales app that syncs with factory app

## Customisation Tab — BUILT (All 5 Phases)
- Wrote full spec: `specs/customisation-tab.md`
- **Built all 5 phases in one session** — 2,523 lines across 8 files
- Code sat locally uncommitted — pushed to GitHub on 2026-02-26 morning
- Render auto-deploys from GitHub push
- **Migration 011_custom_orders.sql** needs to run on DB (check if server.js auto-runs it)
- Files: `src/routes/customOrders.js` (21KB), `src/routes/presentation.js` (11KB), `src/middleware/upload.js`, `db/migrations/011_custom_orders.sql`
- Modified: `server.js`, `public/app.js` (+887 lines), `public/style.css` (+499 lines), `public/index.html`
- Three flagged issues turned out to already be handled in the code (db import, pagination, sequential status transitions)

### LESSON: Always update memory files after completing work. Future-me has no memory without written notes.
