# 2026-02-28

## Sales App — Pull-to-Refresh REMOVED
- **Request:** Remove pull-to-refresh from sales app (it was causing issues)
- **Misunderstanding:** I had added custom pull-to-refresh thinking Owain wanted it; he actually wanted it DISABLED
- **First attempt (failed):**
  - Removed `#pull-indicator` and `#refresh-overlay` HTML elements
  - Removed all pull-to-refresh CSS (indicators, overlay, animations)
  - Removed `setupPullToRefresh()` function from app.js
  - Removed `setupCustomOrdersPullRefresh()` function from app.js
  - **Kept:** `overscroll-behavior-y: contain` on html/body/.app-main/.page
  - **Problem:** Native browser pull-to-refresh still happening
  - **Committed:** `ff224d3` - didn't work
- **Second attempt (Opus analysis + demand planner approach):**
  - **Root cause:** Body element itself was scrollable, triggering native browser pull-to-refresh
  - **Solution:** Copy demand planner approach - lock body scroll, only scroll inner containers
  - **Changes:**
    - `html`: Added `overflow: hidden; height: 100%;`
    - `body`: Changed from `min-height: 100dvh;` to `overflow: hidden; height: 100vh;`
    - `.screen`: Changed from `min-height: 100dvh;` to `height: 100vh; overflow: hidden;`
    - `#screen-app`: Changed from `min-height: 100dvh;` to `height: 100vh; overflow: hidden;`
    - `.app-main`: Already had `flex: 1; overflow-y: auto;` (scroll container)
  - **Result:** Document/body cannot scroll, only `.app-main` scrolls (same as demand planner)
  - **Committed & pushed:** `d06d7f2` - Deployed successfully
- **Third attempt (cache-busting):**
  - **Problem:** Deployment worked, but browsers were caching old `style.css`
  - **Diagnosis:** Used browser tool to inspect live site, confirmed old CSS being served
  - **Confirmed:** Fetching `/style.css?v=20260228` showed NEW CSS with `overflow: hidden` rules
  - **Root cause:** Browser cache serving stale CSS file
  - **Fix:** Added cache-busting to index.html:
    - Changed `<link rel="stylesheet" href="/style.css">` to `href="/style.css?v=2026022802"`
    - Updated app.js version from v=5 to v=6
  - **Committed & pushed:** `899b36e` - This will force browsers to fetch fresh CSS/JS

## Demand Planner — Pull-to-Refresh Added
- **Request:** Add pull-to-refresh to demand planner app (same as sales app)
- **Implementation:**
  - Added pull indicator HTML element (`#pull-indicator`)
  - Added refresh overlay with spinner (`#refresh-overlay`)
  - CSS: Disabled native pull-to-refresh with `overscroll-behavior-y: contain` on html, body, .main, .page-content
  - CSS: Styled pull indicator (sticky, blue background)
  - CSS: Styled overlay (semi-transparent, blurred backdrop, spinning icon)
  - JavaScript: Added `setupPullToRefresh()` function
  - Touch events: touchstart (detect scroll position), touchmove (show indicator), touchend (trigger refresh)
  - On pull-down (>70px), shows overlay and calls `loadPage(currentPage)` to refresh current view
  - Content stays visible during refresh (no blank screen)
- **Committed & pushed:** `45dfdc9` - Render deploying now
- **Location:** `repos/demand-planner/`

## Disabled Status Update Reminders
- **Problem:** Cron job sending "status update" reminder every 2 hours even when no work was happening
- **Root cause:** Cron job `d7b85728-3de0-4e20-abc7-ccbc98c9c273` (name: "2hr Status Update") configured with `everyMs: 7200000` (2 hours)
- **Fix:** Set `enabled: false` in `C:\Users\User\.openclaw\cron\jobs.json`
- **Applied:** Restarted OpenClaw Gateway scheduled task to apply changes
- **Outcome:** No more empty status pings when idle

## Demand Planner — Zoho Direct API Migration (COMPLETE)
- **Goal:** Switch from Make.com webhook to direct Zoho API for all endpoints
- **Credentials:** OAuth (client ID, secret, refresh token) in Render environment variables
- **Organization ID:** 689159620 (ARTICO PTY LTD)

### What Was Done
1. **Added OAuth environment variables to Render:**
   - `ZOHO_CLIENT_ID`, `ZOHO_CLIENT_SECRET`, `ZOHO_REFRESH_TOKEN`
   - `ZOHO_ORG_ID`, `ZOHO_API_DOMAIN`, `ZOHO_ACCOUNTS_URL`
2. **Removed Make.com integration:**
   - Deleted `ZOHO_WEBHOOK_URL` from Render environment variables
   - Updated documentation to remove Make.com references
   - Committed changes (b306b99)
3. **Verified working:**
   - Composite items sync completed successfully
   - 1,366 component mapping records synced from Zoho API
   - Token refresh working automatically
   - All composite → component relationships populated

### API Endpoints (Direct OAuth)
- Token: `POST https://accounts.zoho.com/oauth/v2/token`
- Items: `GET https://www.zohoapis.com/books/v3/items?organization_id=689159620`
- Invoices: `GET https://www.zohoapis.com/books/v3/invoices?organization_id=689159620`
- Composite items: `GET https://www.zohoapis.com/inventory/v1/compositeitems?organization_id=689159620`

### Architecture
- **`src/services/zoho.js`**: Handles OAuth token refresh, provides `zohoGet()` and `zohoPaginateAll()` functions
- **`src/services/sync.js`**: Uses direct API for all syncs (items, sales, composite mappings)
- **No external webhooks**: All API access is direct from Render to Zoho

### Status
✅ **Production deployment complete** - All Make.com references removed, running on direct OAuth only

---

## Demand Planner — 365-Day Sales Sync (PARTIAL - Resume Tomorrow)

**Started:** Mar 1, 2026 at 08:16 AM  
**Stopped:** Mar 1, 2026 at 09:02 AM (hit Zoho API rate limit)

**Progress:**
- ✅ **8 months synced:** Feb 2026 → May 2025 (~4,200 API calls)
- ⏳ **4 months remaining:** Apr 2025 → Mar 2025 (~1,500 API calls)

**Error:** Zoho's 10,000 API calls/day limit exceeded at 4,200 calls

**Fix Applied:**
- Smart rate limiting (200ms delays)
- Exponential backoff retry on 429 errors
- Migration tracking system (runs migrations once only)
- Duplicate-skipping code (ON CONFLICT ... DO NOTHING)

**Next Step (Mar 2, 2026):**
1. Wait 24 hours for Zoho rate limit to reset
2. Trigger same full 365-day sync
3. Duplicate-skipping will skip already-synced months (May→Feb)
4. Only fetch remaining 4 months (~1,500 calls, 10-15 minutes)
5. Complete with full year of sales history for seasonality analysis

**Current Data:** 8 months is already usable for forecasting - full year will improve accuracy
